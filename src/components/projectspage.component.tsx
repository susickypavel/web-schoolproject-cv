import * as React from "react";

interface IProps {
    example: string;
}

export default class ProjectPage extends React.Component<IProps> {
    render() {
        return(
            <div>
                Project page
            </div>
        );
    }
}
